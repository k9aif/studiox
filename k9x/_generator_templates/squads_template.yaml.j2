# SPDX-License-Identifier: Apache-2.0
# Generated by K9-AIF Generator on {{ timestamp }}
#
# Each flow step must be a dict with an 'agent:' key.
# Optional per-step fields: result_key, context, when

squads:
  {{ app_name }}Squad:
    description: "{{ app_name }} agent pipeline"
    agents:
{% for agent in agents %}
      - {{ agent.class }}
{% endfor %}
    flow:
{% for agent in agents %}
      - agent: {{ agent.class }}
        result_key: {{ agent.id }}
{% endfor %}
