# SPDX-License-Identifier: Apache-2.0
# Generated by K9-AIF Generator on {{ timestamp }}
# Agent YAML for {{ agent.class }}
# Loaded by AgentLoader from agents/yaml/

name: {{ agent.class }}
class: {{ agent.class }}

description: >
  {{ agent.class }} — generated stub. Update with domain-specific description.

pattern: reasoning
model: general

role: >
  You are a specialist agent responsible for {{ agent.id | replace("_", " ") }} tasks.

goal: >
  Execute {{ agent.id | replace("_", " ") }} accurately and return a structured result.

instructions:
  - Analyse the input payload carefully
  - Always include a confidence score in your output
  - Return a structured result matching the output_schema

output_schema:
  agent: string
  output: string
  confidence: float (0.0–1.0)

tools: []

governance:
  pre_process: false
  post_process: false
