# SPDX-License-Identifier: Apache-2.0
# Generated by k9x_studio on {{ timestamp }}

squads:
  {{ squad.name }}:
    description: "{{ squad.name }} agent pipeline — {{ domain }}"
    agents:
{% for agent_name in squad.agents %}
      - {{ agent_name }}
{% endfor %}
    flow:
{% for agent_name in squad.agents %}
      - agent: {{ agent_name }}
        result_key: {{ agent_name | lower | replace('agent', '_agent') | trim('_') }}
{% endfor %}
